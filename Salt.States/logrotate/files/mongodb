/var/log/mgolog/*.log /var/log/mongo/*.log /logs/*/*.log /logs/*.log /log/*/*.log /log/*.log{
    daily
    rotate 7
    compress
    delaycompress
    missingok
    create
    sharedscripts
    postrotate
        /usr/bin/pkill -SIGUSR1 mongod
    endscript
}
